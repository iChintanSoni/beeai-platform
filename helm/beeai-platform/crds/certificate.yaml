apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: beeai-platform-tls
  namespace: default
spec:
  commonName: beeai-platform
  dnsNames:
    - beeai-platform
    - beeai-platform.default
    - beeai-platform.default.svc
    - beeai-platform.default.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: default-issuer
  secretName: beeai-platform-tls
